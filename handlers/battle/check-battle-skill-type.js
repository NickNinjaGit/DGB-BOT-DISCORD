module.exports = async function checkBattleSkillType(skillType, skillValue, StatusChangeType, SkillMultiplier) {
    if (skillType === 'DAMAGE') {
    }
    if (skillType === 'HEAL') {
    }
    if (skillType === 'BUFF') {
    }
    if (skillType === 'DEBUFF') {
    }
}